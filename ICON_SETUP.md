# Настройка иконки приложения

## ✅ Выполнено автоматически

1. Файл `appIcon.icns` скопирован в `HSIView/appIcon.icns`
2. `Info.plist` обновлен с добавлением `CFBundleIconFile`

## 🔧 Требуется выполнить в Xcode

### Шаг 1: Добавить файл иконки в проект

1. Откройте проект в Xcode:
   ```bash
   open HSIView.xcodeproj
   ```

2. В навигаторе проекта (левая панель) найдите папку `HSIView`

3. Перетащите файл `HSIView/appIcon.icns` в папку `HSIView` в навигаторе проекта
   - Или: Правый клик на папку `HSIView` → Add Files to "HSIView"...
   - Выберите `appIcon.icns`

4. В появившемся диалоге убедитесь, что:
   - ✅ "Copy items if needed" - **НЕ ОТМЕЧЕНО** (файл уже в нужном месте)
   - ✅ "Create groups" - отмечено
   - ✅ Target "HSIView" - отмечен

### Шаг 2: Проверить Build Phases

1. Выберите проект в навигаторе (самый верхний элемент)
2. Выберите target "HSIView"
3. Перейдите на вкладку "Build Phases"
4. Раскройте секцию "Copy Bundle Resources"
5. Убедитесь, что `appIcon.icns` присутствует в списке
   - Если нет: нажмите "+" и добавьте его

### Шаг 3: Пересобрать проект

1. Очистите сборку: Product → Clean Build Folder (⇧⌘K)
2. Пересоберите: Product → Build (⌘B)
3. Запустите приложение: Product → Run (⌘R)

## 🎨 Проверка результата

После сборки иконка должна появиться:
- В Dock при запуске приложения
- В окне приложения (заголовок окна)
- В Finder для .app файла
- В списке открытых приложений (⌘Tab)

## 📁 Структура файлов

```
HSIView/
├── appIcon.icns          ← В корне (оригинал)
└── HSIView/
    ├── appIcon.icns      ← В папке проекта (копия для сборки)
    └── Info.plist        ← Обновлен с CFBundleIconFile
```

## ⚙️ Что было добавлено в Info.plist

```xml
<key>CFBundleIconFile</key>
<string>appIcon</string>
```

**Примечание:** Расширение `.icns` в строке НЕ указывается - macOS добавит его автоматически.

## 🐛 Решение проблем

### Иконка не отображается

1. **Проверьте формат файла:**
   ```bash
   file HSIView/appIcon.icns
   ```
   Должно быть: `...icns`, Apple icon image...

2. **Проверьте права доступа:**
   ```bash
   ls -l HSIView/appIcon.icns
   ```
   Должно быть читаемым (r)

3. **Проверьте наличие в Bundle:**
   После сборки:
   ```bash
   ls -la "DerivedData/.../HSIView.app/Contents/Resources/"
   ```
   Должен быть `appIcon.icns`

4. **Очистите кэш иконок macOS:**
   ```bash
   sudo rm -rf /Library/Caches/com.apple.iconservices.store
   killall Dock
   killall Finder
   ```

### Иконка размытая

Убедитесь, что `.icns` содержит все размеры:
- 16x16, 32x32, 64x64, 128x128, 256x256, 512x512, 1024x1024
- @1x и @2x варианты

### Создание .icns из изображения

Если нужно создать новый `.icns` из PNG:

```bash
# Создать iconset
mkdir appIcon.iconset

# Скопировать изображения нужных размеров
cp icon_16x16.png appIcon.iconset/icon_16x16.png
cp icon_32x32.png appIcon.iconset/icon_16x16@2x.png
cp icon_32x32.png appIcon.iconset/icon_32x32.png
cp icon_64x64.png appIcon.iconset/icon_32x32@2x.png
cp icon_128x128.png appIcon.iconset/icon_128x128.png
cp icon_256x256.png appIcon.iconset/icon_128x128@2x.png
cp icon_256x256.png appIcon.iconset/icon_256x256.png
cp icon_512x512.png appIcon.iconset/icon_256x256@2x.png
cp icon_512x512.png appIcon.iconset/icon_512x512.png
cp icon_1024x1024.png appIcon.iconset/icon_512x512@2x.png

# Создать .icns
iconutil -c icns appIcon.iconset
```

## 📝 Альтернативный метод (через Assets)

Если предпочитаете использовать Assets.xcassets:

1. Извлеките PNG из `.icns` или используйте оригинальные PNG
2. В Xcode откройте `Assets.xcassets/AppIcon.appiconset`
3. Перетащите соответствующие изображения в слоты
4. Удалите `CFBundleIconFile` из `Info.plist`

**Преимущества текущего подхода (.icns):**
- ✅ Проще (один файл)
- ✅ Все размеры в одном месте
- ✅ Традиционный способ для macOS

**Преимущества Assets подхода:**
- ✅ Больше контроля над размерами
- ✅ Интеграция с Xcode
- ✅ Возможность разных иконок для разных режимов

## ✅ Checklist

- [x] `appIcon.icns` скопирован в `HSIView/`
- [x] `Info.plist` обновлен
- [ ] Файл добавлен в Xcode проект (делается вручную)
- [ ] Проверено в Build Phases → Copy Bundle Resources
- [ ] Проект пересобран
- [ ] Иконка отображается в запущенном приложении

---

**После выполнения шагов в Xcode, иконка будет отображаться во всех местах!** 🎨

