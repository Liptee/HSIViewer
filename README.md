# HSIView

<p align="center">
  <img src="https://img.shields.io/badge/platform-macOS-lightgrey.svg" alt="Platform">
  <img src="https://img.shields.io/badge/Swift-5.9+-orange.svg" alt="Swift">
  <img src="https://img.shields.io/badge/Xcode-15.0+-blue.svg" alt="Xcode">
  <img src="https://img.shields.io/badge/macOS-11.0+-green.svg" alt="macOS">
</p>

**Нативный просмотрщик и процессор гиперспектральных изображений для macOS (Apple Silicon).**

Быстрая визуализация, обработка и экспорт гиперспектральных данных с оптимизацией памяти и производительности.

---

## ✨ Возможности

### 📂 Поддержка форматов

#### Загрузка
- ✅ **NumPy (.npy)**: все типы данных, C/Fortran order, файлы любого размера
- ✅ **MATLAB (.mat)**: переменные, все числовые типы, column-major
- ✅ **TIFF (.tiff)**: многостраничный, CONTIG/SEPARATE, UInt8/UInt16
- ✅ **ENVI (.dat + .hdr)**: BSQ/BIL/BIP, все типы, byte order, wavelengths

#### Экспорт
- ✅ **NumPy (.npy)**: сохраняет тип данных и порядок
- ✅ **PNG Channels**: каждый канал как отдельный PNG (UInt8/UInt16)
- ✅ **Wavelengths**: экспорт длин волн в .txt файл

### 🎨 Визуализация

- ✅ **Grayscale**: поканальный просмотр с слайдером
- ✅ **RGB синтез**: автоматический выбор каналов по длинам волн (R≈630nm, G≈530nm, B≈450nm)
- ✅ **Zoom & Pan**: трекпад масштабирование, перетаскивание, кнопка "Центрировать"
- ✅ **Keyboard navigation**: стрелки для перемещения, shortcuts для команд

### 🔧 Обработка данных

- ✅ **Пайплайн обработки**: последовательное применение операций с drag & drop
  - **Нормализация** (7 типов): Min-Max, Custom, Percentile, Z-Score, Log, Sqrt
  - **Конвертация типов**: Float64/32, Int8/16/32, UInt8/16
  - **Автоматический режим** (⚡): изменения применяются мгновенно
  - **Ручной режим** (✋): выстраивание пайплайна с последующим применением
  - **Drag & Drop**: изменение порядка операций перетаскиванием
  - **Сохранение типа данных**: опция для сохранения типа после нормализации

### 💾 Оптимизация памяти

- ✅ **Хранение в оригинальных типах**: UInt8, Float32, Int16 и др. (экономия до 8x)
- ✅ **Умная нормализация**: сохраняет тип данных при совместимых диапазонах
- ✅ **Кэширование**: статистики вычисляются один раз

### 📊 Информация

- ✅ **Автообновляемая панель**: тип данных, разрешение, память, статистика
- ✅ **Wavelengths**: загрузка из .txt или .hdr, генерация диапазона
- ✅ **Детальная статистика**: min, max, mean, std для текущего состояния куба

### 🖱️ Интерфейс

- ✅ **Keyboard shortcuts**: Cmd+O (открыть), Cmd+E (экспорт), стрелки (навигация)
- ✅ **Finder integration**: двойной клик на файлах .mat, .npy, .tiff, .dat, .hdr
- ✅ **Drag & Drop**: перетаскивание файлов в окно
- ✅ **Современный UI**: SwiftUI, нативный macOS дизайн

---

## 🚀 Быстрый старт

### Требования

- **macOS** 11.0+ (рекомендуется 15.0+)
- **Apple Silicon** (M1/M2/M3/M4)
- **Xcode** 15.0+

### Установка зависимостей

```bash
# Установить Homebrew (если нет)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установить библиотеки
brew install libmatio libtiff
```

### Сборка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd HSIView
```

2. **Откройте проект в Xcode:**
```bash
open HSIView.xcodeproj
```

3. **Настройте зависимости:**
   - Убедитесь, что в Build Settings → Header Search Paths есть `/opt/homebrew/include`
   - Убедитесь, что в Build Settings → Library Search Paths есть `/opt/homebrew/lib`

4. **Соберите проект:**
   - Product → Build (Cmd+B)
   - Или: Product → Run (Cmd+R)

5. **Создайте архив (опционально):**
   - Product → Archive
   - Distribute App → Copy App

---

## 📖 Использование

### Открытие файла

**Способ 1:** Меню
```
File → Открыть... (Cmd+O) → Выберите файл
```

**Способ 2:** Drag & Drop
```
Перетащите файл (.mat, .npy, .tiff, .dat, .hdr) в окно
```

**Способ 3:** Finder
```
Двойной клик на файле
Или: Правый клик → Открыть с помощью → HSIView
```

---

### Просмотр данных

#### Grayscale режим
```
1. Откройте гиперкуб
2. Режим = Gray (по умолчанию)
3. Используйте слайдер для переключения каналов
4. Layout = Auto (обычно подходит)
```

#### RGB режим
```
1. Откройте гиперкуб с wavelengths
   (ENVI автоматически загружает из .hdr)
2. Или загрузите wavelengths вручную
3. Переключитесь: Mode = RGB
4. ✅ Автоматический синтез цветного изображения
```

---

### Обработка (Pipeline)

#### Пример: Нормализация + Конвертация
```
1. Открыть гиперкуб
2. В левой панели нажать "+"
3. Добавить "Нормализация"
   - Тип: Min-Max (custom)
   - Диапазон: 0 → 255
   - ☑️ Сохранить тип данных
4. Добавить "Тип данных"
   - Тип: UInt8
5. ✅ Изменения применяются автоматически (режим ⚡)
6. Проверить Info панель: Тип = UInt8, память уменьшилась
```

#### Режимы пайплайна
- **⚡ Автоматический**: изменения применяются сразу (для быстрых операций)
- **✋ Ручной**: выстроить пайплайн → нажать "Применить" (для тяжелых кубов)

#### Drag & Drop
```
Перетаскивайте операции для изменения порядка
Это важно, т.к. порядок влияет на результат!
```

---

### Экспорт

#### NumPy (.npy)
```
1. File → Экспорт... (Cmd+E)
2. Выбрать "NumPy (.npy)"
3. ☑️ Экспортировать длины волн
4. Нажать "Экспорт"
5. Выбрать путь и сохранить
6. ✅ Создаётся .npy + _wavelengths.txt
```

Проверка в Python:
```python
import numpy as np

cube = np.load('hypercube.npy')
print(cube.shape, cube.dtype)

wavelengths = np.loadtxt('hypercube_wavelengths.txt')
```

#### PNG Channels
```
1. Конвертировать в UInt8 (если нужно)
2. File → Экспорт
3. Выбрать "PNG Channels"
4. Указать базовое имя: "cube"
5. ✅ Создаются: cube_channel_000.png, cube_channel_001.png, ...
```

---

### Zoom & Navigation

- **Масштабирование**: Трекпад (pinch)
- **Перемещение**: Перетаскивание мышью
- **Центрирование**: Кнопка "Центрировать" или сброс zoom
- **Клавиши**: Стрелки (←↑↓→) для перемещения

---

## 🏗️ Архитектура

```
HSIView/
├── Models/              # Модели данных
│   ├── HyperCubeModel.swift      # HyperCube, DataStorage
│   ├── CubeNormalization.swift   # Типы нормализации
│   ├── PipelineOperation.swift   # Операции пайплайна
│   ├── EnviHeader.swift          # ENVI заголовки
│   └── ImageLoadError.swift      # Ошибки загрузки
│
├── Services/            # Загрузчики форматов
│   ├── ImageLoader.swift         # Протокол загрузчиков
│   ├── NpyImageLoader.swift      # NumPy
│   ├── MatImageLoader.swift      # MATLAB
│   ├── TiffImageLoader.swift     # TIFF
│   └── EnviImageLoader.swift     # ENVI
│
├── Exporters/           # Экспортёры форматов
│   ├── NpyExporter.swift         # NumPy экспорт
│   └── TiffExporter.swift        # PNG экспорт
│
├── Utilities/           # Утилиты
│   ├── ImageRenderer.swift       # Рендеринг изображений
│   ├── DataTypeConverter.swift   # Конвертация типов
│   ├── WavelengthManager.swift   # Работа с wavelengths
│   └── DataNormalization.swift   # Нормализация (legacy)
│
├── Views/               # UI компоненты
│   ├── ContentView.swift         # Главное окно
│   ├── ImageInfoPanel.swift      # Информационная панель
│   ├── PipelinePanel.swift       # Панель пайплайна
│   └── ExportView.swift          # Окно экспорта
│
├── Extensions/          # Расширения
│   └── HyperCube+Statistics.swift
│
└── C Helpers/           # C библиотеки
    ├── MatHelper.h/c            # MATLAB wrapper
    └── TiffHelper.h/c           # TIFF wrapper
```

Подробнее: `docs/ARCHITECTURE.md`

---

## 🎯 Примеры использования

### Пример 1: Просмотр ENVI файла
```
1. Двойной клик на .dat или .hdr файл
2. ✅ Автоматически находит парный файл
3. ✅ Загружает wavelengths из .hdr
4. Переключиться в RGB режим
5. ✅ Цветное изображение с правильными каналами
```

### Пример 2: Обработка для ML
```
1. Открыть гиперкуб
2. Пайплайн:
   a) Нормализация: Percentile (2%, 98%)
   b) Нормализация: Min-Max [0, 1]
   c) Тип данных: Float32
3. File → Экспорт → NumPy
4. ✅ Готовый датасет для PyTorch/TensorFlow
```

### Пример 3: Визуализация с настройкой
```
1. Открыть .mat файл
2. Пайплайн:
   a) Нормализация: Z-Score (убрать outliers)
   b) Нормализация: Min-Max [0, 255]
   c) Тип данных: UInt8
3. Переключаться между каналами слайдером
4. ✅ Чёткое изображение без шума
```

---

## 📊 Форматы данных

### NumPy (.npy)

**Загрузка:**
- Типы: float32/64, int8/16/32, uint8/16
- C-order и Fortran-order
- 2D и 3D массивы

**Экспорт:**
- Сохраняет оригинальный тип
- Сохраняет порядок данных
- Wavelengths в .txt

```python
# Создание:
import numpy as np
data = np.random.rand(512, 217, 204).astype(np.float32)
np.save('hypercube.npy', data)

# Чтение экспортированного:
cube = np.load('exported.npy')
wavelengths = np.loadtxt('exported_wavelengths.txt')
```

---

### MATLAB (.mat)

**Загрузка:**
- Формат: MAT v5/v7.3
- Типы: double, single, uint8/16, int8/16/32
- Первая найденная 3D переменная

```matlab
% Создание:
data = rand(512, 217, 204, 'single');
save('hypercube.mat', 'data');

% Примечание: MATLAB использует column-major (Fortran order)
```

---

### TIFF (.tiff)

**Загрузка:**
- Многостраничный TIFF (каждая страница = канал)
- PLANARCONFIG_SEPARATE или CONTIG
- UInt8 и UInt16

**Экспорт:**
- Каждый канал → отдельный PNG файл
- Только UInt8/UInt16

---

### ENVI (.dat + .hdr)

**Особенности:**
- Парные файлы в одной директории
- Автоматический поиск парного файла
- Wavelengths из .hdr файла
- Interleave: BSQ, BIL, BIP
- Byte order: little/big endian
- Все типы данных

**Поддерживаемые расширения:**
- Header: `.hdr`
- Binary: `.dat`, `.img`, `.bsq`, `.bil`, `.bip`, `.raw`

```
Примеры файлов:
- image.hdr + image.dat
- scene.hdr + scene.img
- data.hdr + data.bsq
```

---

## 🔧 Пайплайн обработки

### Доступные операции

#### 1. Нормализация (7 типов)

| Тип | Описание | Параметры |
|-----|----------|-----------|
| **None** | Без нормализации | - |
| **Min-Max** | [0, 1] | - |
| **Min-Max (custom)** | Свой диапазон | min, max |
| **Percentile** | Обрезка outliers | lower %, upper % |
| **Z-Score** | Стандартизация | - |
| **Log** | Логарифм | - |
| **Sqrt** | Корень | - |

#### 2. Тип данных (7 типов)

- Float64 (Double), Float32
- Int8 (-128...127), Int16, Int32
- UInt8 (0-255), UInt16 (0-65535)

**Опции:**
- ☑️ Автоматическое масштабирование (значения масштабируются)
- ☐ Clamping (значения обрезаются)

---

### Примеры пайплайнов

#### Подготовка для ML
```
1. Percentile (2%, 98%)     → убрать outliers
2. Min-Max [0, 1]           → нормализовать
3. Float32                  → уменьшить размер
   ✅ Результат: готовый датасет
```

#### Визуализация
```
1. Z-Score                  → стандартизация
2. Min-Max [0, 255]         → диапазон для изображения
3. UInt8                    → экономия памяти 8x
   ☑️ Сохранить тип данных
   ✅ Результат: чёткое изображение, малый размер
```

#### Экспорт в TIFF
```
1. Любая нормализация
2. Min-Max [0, 255]
3. UInt8 (обязательно!)
4. Экспорт → PNG Channels
   ✅ Совместимо с ImageJ, GIMP, etc.
```

---

## ⌨️ Keyboard Shortcuts

| Shortcut | Действие |
|----------|----------|
| `Cmd+O` | Открыть файл |
| `Cmd+E` | Экспорт |
| `←↑↓→` | Перемещение по масштабированному изображению |
| `Esc` | Закрыть модальное окно |
| `Enter` | Подтвердить в модальном окне |

---

## 💡 Советы и трюки

### Экономия памяти
```
✅ Используйте UInt8 для визуализации (экономия 8x)
✅ Float32 вместо Float64 для ML (экономия 2x)
✅ Включайте "Сохранить тип данных" при нормализации
```

### Производительность
```
✅ Ручной режим пайплайна для больших кубов (>1 GB)
✅ Применяйте операции последовательно, не добавляйте всё сразу
```

### ENVI файлы
```
✅ Оба файла (.hdr + .dat) должны быть в одной папке
✅ Можно открывать любой из них
✅ При первом открытии разрешите доступ к директории
```

---

## 📚 Документация

### Основная документация
- [CHANGELOG.md](CHANGELOG.md) - История изменений
- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) - Архитектура проекта
- [docs/DEVELOPER_GUIDE.md](docs/DEVELOPER_GUIDE.md) - Руководство разработчика

### Форматы данных
- [docs/NPY_SUPPORT.md](docs/NPY_SUPPORT.md) - NumPy формат
- [docs/ENVI_SUPPORT.md](docs/ENVI_SUPPORT.md) - ENVI формат
- [docs/MAT_INTEGER_SUPPORT.md](docs/MAT_INTEGER_SUPPORT.md) - MATLAB целочисленные типы
- [docs/TIFF_CONTIG_SUPPORT.md](docs/TIFF_CONTIG_SUPPORT.md) - TIFF CONTIG формат

### Функции
- [docs/PIPELINE_SYSTEM.md](docs/PIPELINE_SYSTEM.md) - Система пайплайнов
- [docs/NORMALIZATION_FEATURE.md](docs/NORMALIZATION_FEATURE.md) - Нормализация
- [docs/ZOOM_NAVIGATION.md](docs/ZOOM_NAVIGATION.md) - Zoom и навигация
- [docs/EXPORT_SETUP.md](docs/EXPORT_SETUP.md) - Экспорт файлов

### Исправления и оптимизации
- [docs/MEMORY_OPTIMIZATION.md](docs/MEMORY_OPTIMIZATION.md) - Оптимизация памяти
- [docs/NPY_LARGE_FILES_FIX.md](docs/NPY_LARGE_FILES_FIX.md) - Большие NPY файлы
- [docs/FINDER_OPEN_FIX.md](docs/FINDER_OPEN_FIX.md) - Finder интеграция

---

## 🧪 Тестирование

Тестовые данные находятся в `test_data/`:
- `Salinas_corrected.mat` - классический HSI датасет
- `sponges.npy` - NumPy формат
- `Tablet.tiff` - TIFF CONTIG
- `ang20200709t213509.dat/.hdr` - ENVI BIP
- `REFLECTANCE_075.dat/.hdr` - ENVI BSQ

Python скрипты для тестирования: `python_tests/`

---

## 🐛 Известные ограничения

- MATLAB (.mat) экспорт временно отключён (требует настройки)
- PNG Channels создаёт много файлов (по одному на канал)
- TIFF экспорт только для UInt8/UInt16

---

## 🔄 Миграция из старых версий

### v0.1 → v0.4
- Удалены старые панели "Нормализация" и "Тип данных"
- Вся обработка теперь через Пайплайн (левая панель)
- Добавлена поддержка ENVI формата
- Добавлен экспорт

---

## 🛠️ Разработка

### Добавление нового формата

```swift
class MyFormatLoader: ImageLoader {
    static let supportedExtensions = ["xyz"]
    
    static func load(from url: URL) -> Result<HyperCube, ImageLoadError> {
        // 1. Прочитать файл
        // 2. Распарсить header
        // 3. Загрузить данные в DataStorage
        // 4. Вернуть HyperCube
        return .success(HyperCube(
            dims: (height, width, channels),
            storage: .uint8(data),
            sourceFormat: "My Format",
            isFortranOrder: false,
            wavelengths: wavelengths
        ))
    }
}

// Добавить в ImageLoader.swift:
private static let loaders: [ImageLoader.Type] = [
    // ... существующие
    MyFormatLoader.self
]
```

Подробнее: `docs/DEVELOPER_GUIDE.md`

---

## 📦 Зависимости

### Системные библиотеки
- **libmatio** (1.5.23+) - чтение MATLAB файлов
- **libtiff** (4.5.0+) - чтение TIFF файлов
- **CoreGraphics** - рендеринг изображений
- **SwiftUI** - пользовательский интерфейс

### Установка
```bash
brew install libmatio libtiff
```

### Проверка
```bash
brew list libmatio libtiff
ls -la /opt/homebrew/lib/libmatio.dylib
ls -la /opt/homebrew/lib/libtiff.dylib
```

---

## 🤝 Контрибьюция

Contributions welcome!

1. Fork репозиторий
2. Создайте feature branch: `git checkout -b feature/my-feature`
3. Commit изменения: `git commit -m 'Add amazing feature'`
4. Push в branch: `git push origin feature/my-feature`
5. Откройте Pull Request

**Перед PR:**
- ✅ Убедитесь, что код компилируется
- ✅ Протестируйте на нескольких файлах
- ✅ Обновите CHANGELOG.md
- ✅ Добавьте документацию в `docs/`

---

## 📝 Версии

### v0.4.0 (2025-11-28) - Пайплайн и экспорт
- ✨ Система пайплайнов обработки с drag & drop
- ✨ Экспорт в NumPy и PNG
- ✨ Нормализация (7 типов) с сохранением типа данных
- ✨ Авто/ручной режимы пайплайна
- 🐛 Множество исправлений и оптимизаций

### v0.3.0 (2025-11-27) - ENVI и UI
- ✨ Поддержка ENVI формата (.dat + .hdr)
- ✨ Zoom, Pan, навигация клавишами
- ✨ Автообновление информационной панели
- ✨ Конвертация типов данных

### v0.2.0 (2025-11-27) - Оптимизация
- ✨ Оптимизация памяти (до 8x экономии)
- ✨ Поддержка больших NPY файлов (>2GB)
- ✨ TIFF CONTIG, MAT integer types
- ✨ Улучшенный UI (File меню, keyboard shortcuts)

### v0.1.0 (2025-11-26) - Initial Release
- ✨ Базовый просмотр .mat и .tiff
- ✨ Поканальный режим и RGB синтез

**Полная история:** [CHANGELOG.md](CHANGELOG.md)

---

## 📄 Лицензия

MIT License (или ваша лицензия)

---

## 🙏 Благодарности

- [libmatio](https://github.com/tbeu/matio) - чтение MATLAB файлов
- [libtiff](http://www.libtiff.org/) - чтение TIFF файлов
- SwiftUI team за отличный framework
- Hyperspectral imaging community

---

## 📧 Контакты

Вопросы или предложения? Создайте [Issue](../../issues) в репозитории.

---

<p align="center">
  <strong>Made with ❤️ for hyperspectral imaging community</strong>
</p>
