# Реализация информационной панели

## ✅ Что реализовано

Добавлена информационная панель справа от изображения, которая отображает:

### 1. Характеристики изображения
- **Формат источника**: MATLAB (.mat) или TIFF (.tiff)
- **Тип данных**: Float64, Float32, UInt8 и т.д.
- **Разрешение**: размеры куба (например, 100 × 512 × 512)
- **Количество каналов**: автоматически определяется по Layout

### 2. Статистика данных
- **Минимальное значение**: по всему гиперкубу
- **Максимальное значение**: по всему гиперкубу
- **Среднее**: среднее арифметическое
- **Стандартное отклонение**: мера разброса данных

### 3. Технические характеристики
- **Размер в памяти**: сколько МБ занимает в оперативной памяти

## 🎨 UI/UX особенности

### Интерактивность
- ✅ Панель можно **свернуть/развернуть** кликом на заголовок
- ✅ Анимированный переход при сворачивании (0.2 сек)
- ✅ Все значения можно **выделить и скопировать**
- ✅ Панель автоматически обновляется при смене Layout

### Дизайн
- **Ширина**: 260 пикселей (фиксированная)
- **Положение**: справа от изображения
- **Scroll**: при недостатке места по вертикали
- **Шрифт**: моноширинный для числовых значений
- **Цвета**: адаптируются к системной теме (светлая/темная)

### Иконки
- `▼` - развернуто
- `▶` - свернуто

## 🔧 Технические изменения

### Новые файлы
1. **Views/ImageInfoPanel.swift** (90 строк)
   - SwiftUI компонент панели
   - Collapsible UI с анимацией
   - Форматирование числовых значений

### Обновленные файлы

#### Models/HyperCubeModel.swift
```swift
// Добавлен enum DataType
enum DataType: String {
    case float64 = "Float64"
    case float32 = "Float32"
    case int8 = "Int8"
    case int16 = "Int16"
    case int32 = "Int32"
    case uint8 = "UInt8"
    case uint16 = "UInt16"
    case unknown = "Unknown"
}

// Обновлена структура HyperCube
struct HyperCube {
    let dims: (Int, Int, Int)
    let data: [Double]
    let originalDataType: DataType      // ← Новое
    let sourceFormat: String            // ← Новое
    
    var resolution: String {            // ← Новое
        "\(dims.0) × \(dims.1) × \(dims.2)"
    }
}
```

#### Services/MatImageLoader.swift
```swift
// Теперь определяет тип данных
let dataType: DataType = .float64  // или .float32

return .success(HyperCube(
    dims: (d0, d1, d2),
    data: arr,
    originalDataType: dataType,
    sourceFormat: "MATLAB (.mat)"
))
```

#### Services/TiffImageLoader.swift
```swift
// TIFF всегда 8-bit
return .success(HyperCube(
    dims: (d0, d1, d2),
    data: arr,
    originalDataType: .uint8,
    sourceFormat: "TIFF (.tiff)"
))
```

#### ContentView.swift
```swift
// Добавлен HStack с панелью справа
HStack(spacing: 0) {
    // Изображение (как было)
    GeometryReader { ... }
    
    // Новая панель справа
    if let cube = state.cube {
        VStack {
            ScrollView {
                ImageInfoPanel(cube: cube, layout: state.layout)
                    .padding(12)
            }
            .frame(width: 260)
        }
    }
}
```

### Обновленные параметры
- **Минимальная ширина окна**: 700 → **960** пикселей (для размещения панели)

## 📊 Пример отображения

```
╔═══════════════════════════════════════╗
║ ▼ Информация об изображении           ║
╠═══════════════════════════════════════╣
║ Формат:         MATLAB (.mat)         ║
║ Тип данных:     Float64               ║
║ Разрешение:     100 × 512 × 512       ║
║ Каналы:         100                   ║
║ ───────────────────────────────────   ║
║ Мин. значение:  0.0234                ║
║ Макс. значение: 0.9876                ║
║ Среднее:        0.4521                ║
║ Станд. откл.:   0.2341                ║
║ ───────────────────────────────────   ║
║ Размер в памяти: 199.2 МБ             ║
╚═══════════════════════════════════════╝
```

## 🔄 Как это работает

### Поток данных

1. **Загрузка файла**
   ```
   User opens file
   → ImageLoader загружает данные
   → Определяет originalDataType и sourceFormat
   → Создает HyperCube с метаданными
   → AppState.cube обновляется
   ```

2. **Отображение панели**
   ```
   ContentView обнаруживает cube
   → Рендерит ImageInfoPanel
   → ImageInfoPanel читает:
      - cube.sourceFormat
      - cube.originalDataType
      - cube.resolution
      - cube.channelCount(for: layout)
   → Вычисляет статистику: cube.statistics()
   → Форматирует и отображает
   ```

3. **Обновление при смене Layout**
   ```
   User меняет Layout
   → state.layout обновляется
   → ImageInfoPanel перерисовывается
   → channelCount пересчитывается автоматически
   → Остальные данные неизменны
   ```

## 🎯 Использование статистики

Панель использует extension `HyperCube+Statistics`:

```swift
let stats = cube.statistics()
// stats.min: Double
// stats.max: Double
// stats.mean: Double
// stats.stdDev: Double
```

Это extension уже было добавлено в предыдущем рефакторинге и теперь активно используется!

## 💡 Преимущества реализации

### 1. Модульность
- `ImageInfoPanel` - независимый компонент
- Легко использовать в других View
- Можно расширять без изменения ContentView

### 2. Реактивность
- Автоматически обновляется при изменении данных
- Подписан на изменения Layout
- SwiftUI обрабатывает перерисовку

### 3. Расширяемость
Легко добавить новые поля:

```swift
// В ImageInfoPanel.swift
infoRow(title: "Новое поле", value: someValue)
```

### 4. Тестируемость
```swift
// Можно создать preview
#Preview {
    ImageInfoPanel(
        cube: mockCube,
        layout: .chw
    )
}
```

## 🚀 Будущие улучшения

Панель готова к расширению:

### Простые добавления
- [ ] Дата создания файла
- [ ] Размер файла на диске
- [ ] Информация о compression (для TIFF)
- [ ] Список переменных (для .mat)

### Сложные добавления
- [ ] Мини-график histogram
- [ ] Статистика текущего канала (а не всего куба)
- [ ] График распределения значений
- [ ] Информация о wavelengths (если загружены)
- [ ] Кнопка экспорта метаданных в JSON

### Интерактивность
- [ ] Клик на пиксель → показать значение
- [ ] Выбор области → статистика ROI
- [ ] Копирование всей информации в clipboard

## 📝 Документация обновлена

- ✅ README.md - добавлен раздел "Информационная панель"
- ✅ CHANGELOG.md - добавлена версия [Unreleased]
- ✅ SCREENSHOT_INFO.md - подробное описание UI
- ✅ INFO_PANEL_IMPLEMENTATION.md - этот файл

## 🎓 Что можно изучить

Этот компонент демонстрирует:
- ✅ Collapsible UI в SwiftUI
- ✅ Использование @State для анимации
- ✅ Форматирование чисел (String(format:))
- ✅ Text selection в SwiftUI
- ✅ Адаптивный дизайн (ScrollView)
- ✅ Композиция View (helper functions)

---

**Панель готова к использованию! Запустите приложение и откройте любой файл.** 🎉

