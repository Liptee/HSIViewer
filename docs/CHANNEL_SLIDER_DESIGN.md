# Channel Slider Design - QuickTime Style

## Обзор

Новый дизайн слайдера для переключения каналов, вдохновленный QuickTime Player, с визуальным превью всех каналов одновременно.

<p align="center">
  <img src="screenshot-placeholder.png" alt="Channel Slider" width="600">
</p>

---

## ✨ Основные особенности

### 1. Визуальное превью каналов

**Слайдер разделен на N сегментов**, где N = количество каналов:
- Каждый сегмент показывает **усредненный цвет канала** в градациях серого
- Сразу видно **яркие и тёмные каналы**
- Легко найти интересующий канал по контрасту

### 2. Дизайн как в QuickTime Player

- **Прямоугольная форма** с закругленными краями (8px radius)
- **Красный ползунок** (4px ширина) для точного позиционирования
- **Высота:** 60px для комфортного взаимодействия
- **Тонкая рамка** для чёткого контура (secondary color, opacity 0.3)

### 3. Плавная анимация

- **Interactive spring** при переключении каналов
- **Response:** 0.3, **Damping:** 0.7
- **Тень** под ползунком для объёмности

### 4. Интерактивность

- **Drag gesture** для перемещения
- **Клик** в любое место слайдера → переход к каналу
- **Мгновенная реакция** на перетаскивание

---

## 🎨 Дизайн спецификация

### Размеры

```
Слайдер:
  - Высота: 60px
  - Ширина: 100% контейнера
  - Border radius: 8px
  - Border width: 1px

Ползунок:
  - Ширина: 4px
  - Высота: 60px (на всю высоту слайдера)
  - Цвет: Красный (Color.red)
  - Border radius: 2px
  - Тень: черная, opacity 0.3, radius 2, offset (0, 1)

Сегменты:
  - Ширина каждого: sliderWidth / channelCount
  - Высота: 52px (60 - 8px для отступов)
  - Spacing: 0 (без зазоров)
```

### Цвета

```swift
Сегменты:
  - Цвет: Color(white: normalizedValue)
  - normalizedValue: 0.0 (черный) → 1.0 (белый)

Ползунок:
  - Цвет: Color.red

Рамка:
  - Цвет: Color.secondary.opacity(0.3)
```

---

## 🔧 Техническая реализация

### Структура компонента

```swift
struct ChannelSliderView: View {
    @Binding var currentChannel: Double
    let channelCount: Int
    let cube: HyperCube?
    
    @State private var channelPreviews: [Color] = []
    @State private var isDragging: Bool = false
    
    var body: some View {
        GeometryReader { geometry in
            ZStack(alignment: .leading) {
                channelPreviewTrack(width: geometry.size.width)
                thumbIndicator(width: geometry.size.width)
            }
            .gesture(dragGesture(width: geometry.size.width))
        }
    }
}
```

### Генерация превью каналов

**Алгоритм:**

```swift
1. Для каждого канала:
   a. Выбрать пиксели с шагом (sampling для оптимизации)
   b. Вычислить среднее значение
   c. Нормализовать в диапазон [0, 1]:
      - UInt8: value / 255
      - UInt16: value / 65535
      - Float: clamp(value, 0, 1)
   d. Создать Color(white: normalizedValue)

2. Сохранить в channelPreviews: [Color]
```

**Оптимизация:**
- **Sampling:** до 10,000 пикселей на канал
- **Background thread:** DispatchQueue.global(qos: .userInitiated)
- **Update on main:** DispatchQueue.main.async для UI

### Обработка жестов

```swift
DragGesture(minimumDistance: 0)
    .onChanged { value in
        let percentage = value.location.x / width
        let newChannel = percentage * Double(channelCount - 1)
        currentChannel = round(newChannel)
    }
```

### Автоматическое обновление

```swift
.onAppear {
    generateChannelPreviews()
}
.onChange(of: cube?.id) { _ in
    generateChannelPreviews()
}
```

Превью пересчитываются:
- При первом появлении
- При смене куба (загрузка нового файла)
- При изменении `cube.id` (после пайплайна)

---

## 📊 Примеры использования

### Пример 1: Найти яркий канал

```
Визуально:
  ████████░░░░░░░░████████
  0      50      100    150

Яркие каналы: 0-50, 100-150
Тёмные каналы: 50-100

Действие:
  Клик на яркий сегмент → переход к каналу
```

### Пример 2: ENVI файл

```
Файл: hypercube (512×217×204)

Слайдер показывает:
  - Каналы 0-50: светлые (visible spectrum)
  - Каналы 50-150: средние (NIR)
  - Каналы 150-204: темные (SWIR)

Польза:
  Сразу видно какие диапазоны использовать для RGB
```

### Пример 3: После нормализации

```
До нормализации:
  Слайдер: ░░░░░░░░░░████████████
  (большинство темные, несколько ярких)

После Min-Max (0-1):
  Слайдер: ████████████████████
  (все каналы равномерно яркие)

Вывод: нормализация сработала!
```

### Пример 4: После поканальной нормализации

```
До:
  Слайдер: ░░░░█░░░░████░░░░░░
  (разные яркости)

После поканальной:
  Слайдер: ████████████████████
  (все каналы выровнены)

Каждый канал теперь использует полный диапазон!
```

---

## 🎯 Преимущества

### По сравнению с обычным слайдером

| Обычный Slider | QuickTime-style Slider |
|----------------|------------------------|
| Просто линия | Визуальное превью |
| Не видно содержимое | Видно все каналы сразу |
| Навигация вслепую | Навигация по визуалу |
| Стандартный вид | Профессиональный дизайн |

### Для пользователя

✅ **Быстрая навигация:** Видно где интересные каналы  
✅ **Визуальная обратная связь:** Понятно что в каждом канале  
✅ **Профессиональный вид:** Как в QuickTime Player  
✅ **Удобство:** Клик в любое место → переход  

### Для анализа

✅ **Видно распределение:** Какие диапазоны яркие/темные  
✅ **Контроль нормализации:** Видно эффект операций  
✅ **RGB выбор:** Легко найти нужные каналы для композита  

---

## 🔄 Интеграция с пайплайном

### Автоматическое обновление

```
Сценарий:
1. Открыть куб
   → Генерируется превью (10 сек)

2. Применить нормализацию
   → cube.id изменился
   → Превью пересчитываются

3. Применить конвертацию типа
   → cube.id изменился
   → Превью пересчитываются

4. Очистить пайплайн
   → Возврат к original cube
   → cube.id изменился
   → Превью пересчитываются
```

### UUID в HyperCube

```swift
struct HyperCube {
    let id: UUID  // ← Позволяет отслеживать изменения
    // ...
}

// В ChannelSliderView:
.onChange(of: cube?.id) { _ in
    generateChannelPreviews()
}
```

---

## ⚡ Оптимизация производительности

### Sampling стратегия

```swift
let sampleSize = min(height * width, 10000)
let step = max(1, (height * width) / sampleSize)

for h in stride(from: 0, to: height, by: step) {
    for w in stride(from: 0, to: width, by: step) {
        // Sample pixel
    }
}
```

**Примеры:**

| Размер канала | Sample size | Step | Время |
|---------------|-------------|------|-------|
| 100×100 = 10K | 10K | 1 | ~0.1s |
| 512×512 = 262K | 10K | 26 | ~0.1s |
| 1024×1024 = 1M | 10K | 100 | ~0.1s |

### Background computation

```swift
DispatchQueue.global(qos: .userInitiated).async {
    // Вычисления (не блокирует UI)
    
    DispatchQueue.main.async {
        channelPreviews = previews  // UI update
    }
}
```

### Кэширование

Превью **не пересчитываются** при:
- Переключении между каналами
- Изменении zoom/pan
- Переключении Gray ↔ RGB

Превью **пересчитываются** только при:
- Загрузке нового файла
- Применении пайплайна

---

## 🎨 Варианты кастомизации

### Изменить высоту

```swift
private let sliderHeight: CGFloat = 60  // ← Изменить здесь
```

### Изменить цвет ползунка

```swift
.fill(Color.red)  // ← Можно заменить на .blue, .green, etc.
```

### Изменить радиус закругления

```swift
.cornerRadius(8)  // ← Изменить для более/менее закругленных краев
```

### Добавить градиент вместо серого

```swift
// Вместо Color(white: grayValue)
LinearGradient(
    colors: [Color.blue, Color.red],
    startPoint: .leading,
    endPoint: .trailing
)
```

---

## 🐛 Известные ограничения

1. **Большое количество каналов** (>500)
   - Сегменты становятся очень узкими
   - Решение: группировка каналов

2. **Очень большие кубы** (>2GB)
   - Генерация превью может занять 5-10 сек
   - Решение: показывать loading indicator

3. **2D изображения**
   - Слайдер не показывается (is2D == true)
   - Это правильное поведение

---

## 🔮 Будущие улучшения

### Планируется:

- [ ] **Loading indicator** во время генерации превью
- [ ] **Tooltip** при hover: показать номер канала и wavelength
- [ ] **Цветовые схемы**: не только grayscale, но и colormaps
- [ ] **Zoom in slider**: увеличение части слайдера для точной навигации
- [ ] **Группировка каналов**: для кубов с >500 каналами
- [ ] **Benchmark overlay**: показать время генерации

---

## 📚 См. также

- [UI_IMPROVEMENTS.md](UI_IMPROVEMENTS.md) - Другие UI улучшения
- [ZOOM_NAVIGATION.md](ZOOM_NAVIGATION.md) - Навигация и масштабирование
- [PIPELINE_SYSTEM.md](PIPELINE_SYSTEM.md) - Система пайплайнов

---

**Версия:** v0.5+  
**Дата:** 2025-11-29  
**Вдохновлено:** QuickTime Player timeline  
**Автор:** HSIView Team


